import 'package:polymer/polymer.dart';

/**
 * A Polymer game2-level1 element.
 */
@CustomTag('game2-level1')

class Game2Level1 extends PolymerElement {

  /// Constructor used to create instance of Game2Level1.
  Game2Level1.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of game2-level1 is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of game2-level1 is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// game2-level1 is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when game2-level1 has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
