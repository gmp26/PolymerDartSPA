import 'package:polymer/polymer.dart';

/**
 * A Polymer code-blockly element.
 */
@CustomTag('code-blockly')

class CodeBlockly extends PolymerElement {

  /// Constructor used to create instance of CodeBlockly.
  CodeBlockly.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of code-blockly is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of code-blockly is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// code-blockly is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when code-blockly has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
