import 'package:polymer/polymer.dart';

/**
 * A Polymer game2-level2 element.
 */
@CustomTag('game2-level2')

class Game2Level2 extends PolymerElement {

  /// Constructor used to create instance of Game2Level2.
  Game2Level2.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of game2-level2 is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of game2-level2 is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// game2-level2 is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when game2-level2 has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
