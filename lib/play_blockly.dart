import 'package:polymer/polymer.dart';

/**
 * A Polymer play-blockly element.
 */
@CustomTag('play-blockly')

class PlayBlockly extends PolymerElement {

  /// Constructor used to create instance of PlayBlockly.
  PlayBlockly.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of play-blockly is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of play-blockly is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// play-blockly is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when play-blockly has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
